#include<stdio.h>

main()
{
	
	float h , b;
	
	printf("Enter the value of (h) : ");
	scanf("%f",&h);
	printf("Enter the value of (b) : ");
	scanf("%f",&b);
	
	printf("(h * b) / 2 = %.2f",(h * b) / 2 );
	
}
